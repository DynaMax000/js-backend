<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Login</title>
  <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
  <style>
    /* Hide Edge/Chrome password reveal button */
    input[type="password"]::-ms-reveal,
    input[type="password"]::-ms-clear,
    input[type="password"]::-webkit-credentials-auto-fill-button,
    input[type="password"]::-webkit-input-placeholder,
    input[type="password"]::-webkit-input-password-reveal-button {
      display: none !important;
    }

    input[type="password"]::-webkit-input-password-reveal {
      display: none !important;
    }
  </style>
</head>

<body>
  <div class="bg-zinc-700 min-w-full min-h-screen min-w-screen flex items-center justify-center">
    <div class="flex p-4 w-full">
      <form class="flex flex-col gap-4 bg-zinc-800 p-8 rounded-lg shadow-lg w-full max-w-md mx-auto" action="/login"
        method="POST">
        <h1 class="text-2xl font-bold mb-4 text-center text-white">Login User</h1>
        <input type="email" name="email" placeholder="Enter email"
          class="p-2 rounded-lg bg-zinc-700 text-white outline-none">
        <input type="password" name="password" class="p-2 rounded-lg bg-zinc-700 text-white outline-none"
          placeholder="Enter password">
        <button type="submit" class="p-2 bg-blue-500 text-white rounded-lg outline-none">Login</button>
        <p class="text-center text-white">New user? | <a href="/" class="text-blue-500">Sign up</a></p>
      </form>
    </div>
  </div>
</body>

</html>